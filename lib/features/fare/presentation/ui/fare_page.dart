import 'package:flutter/material.dart';

class FarePage extends StatelessWidget {
  const FarePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Fare')),
      body: Center(child: Text('Fare')),
    );
  }
}
